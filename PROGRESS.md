# 开发进度与真实记录

## 已确认约定

- 根目录：`C:\Users\ruixuanhu\Desktop\RLtoGame`；三个游戏目录：snake、mario、minecraft。
- 当前采用分阶段教学开发，学习者会 Python、未学深度学习；交付中文资料、Markdown 报告、桌面演示。
- 公开仓库目标为 `555apex/RLtoGame`，main 分支；每次提交/推送等用户明确通知。
- 单蛇预算：DQN / Double DQN 各 3 个训练种子，每次 300,000 环境步，现已全部完成，累计 1,800,000 步；最终测试 220 局已完成。阶段一、二章节保留当时状态，当前状态以最新章节为准。

## 2026-09-09：阶段一实施

### 已完成

- 建立三个游戏目录、长期项目约定、仓库与单蛇 README。
- 建立 `snake/.venv`，在 Python 3.13.6 / Windows 安装并锁定阶段一依赖；不修改系统 PyTorch。
- 实现单蛇 Gymnasium 环境、204 维编码、独立 Pygame 渲染。
- 实现随机和一步启发式策略；基线仅读取观测，随机动作不屏蔽危险。
- 实现桌面人工/自动演示、暂停/单步/重置、无窗口 GIF 与动作轨迹导出。
- 运行验证集种子 1000–1019，每方法 20 局，逐局 CSV、汇总与来源 manifest 已保存。
- 生成中文第一课、带参考答案的复盘题、阶段报告、基线图和观测图。
- 本地 Git 仓库已初始化 main；尚无 commit，尚未推送。

### 实际验证

- 50 项 pytest 检查通过，包括 Gymnasium API、尾格腾空、满盘、不添加饥饿死亡、2000 步截断、奖励、复现与观察副本。
- 桌面事件流程使用 SDL dummy 显示驱动验证暂停、人工动作、单步、重置、退出；另检查真实 RGB 渲染图和导出的 GIF。尚未由学生在真实桌面亲自操作验收。
- 验证均值：随机 0.15 个食物 / 局，启发式 17.25 个食物 / 局；每种 20 局均碰撞，无满盘和截断。是固定策略验证结果，不是 RL 学习结果。
- 种子 1000 启发式：66 步，食物 10 个，最终头部三种候选格均被身体占用。保存 67 帧 GIF、对应 JSONL 和静态预览。
- 1000 步预热后测量 10000 个随机动作，跨 486 次 reset；本次约 28.36 万步/秒。仅计 CPU 环境/随机策略，无渲染与优化器，短测量不能用于保证 DQN 训练时间。
- 图中文字与标记已目视检查，报告数值与原始 CSV 对账；环境/基线源文件哈希在报告重建时校验。
- 收尾重新执行 40 个验证回合，食物数、长度、步数与结束原因全部一致；6 份单蛇 Markdown 文档中的本地链接检查通过。

### 真实取舍与修复

- 食物从一次列出的全部空格均匀采样，避免满盘重试循环；固定 10×10 优先保证可读性。
- 碰撞前计算旧身体占用集合；终局保留最后合法棋盘，终止原因和动作记录碰撞尝试。
- 阶段一固定协议的核心配置必须与实现一致，防止只改 JSON 却以为规则改变。
- 正式测试集未运行；先用验证集建立基线和失败案例，不以演示录像选最终模型。
- Matplotlib 首次默认缓存路径不可写，改用 `snake/.cache/matplotlib`；pytest 临时目录也移到游戏目录中。
- 报告使用用户指定的 Markdown；分析插件仅辅助技术报告结构与图表质量，不额外发布网站。

### 未完成与阻塞

- **远程仓库尚未创建。** 已安装本地 GitHub CLI 2.100.0，连接器能读取账号，但 CLI 无登录态。两次浏览器登录请求在 `github.com/login/device/code` 网络超时，尚未获得设备授权码。不是自动审批拒绝。
- 后续在用户终端执行 `.tools/gh/bin/gh.exe auth login --hostname github.com --git-protocol https --web` 完成登录，再创建公开空仓库。不得把此步骤当作提交或推送许可。
- DQN、Double DQN、GPU 兼容验证、真实训练、模型权重、正式测试和最终报告属于后续阶段。`train.py` 仅给出未实现提示。
- 学生独立阅读、运行和口述尚待完成，不能替学生标记已掌握。

## 2026-09-09：阶段二实施

### 仓库阻塞已解除

- 用户告知已创建仓库；连接器核验 `555apex/RLtoGame` 为公开仓库、默认 main。
- 本地 origin 已关联到该仓库。Git 配置首次写入受沙箱限制，使用授权的本地配置操作完成；没有改全局 safe.directory，没有提交或推送。
- 第一阶段关于“远程仓库未创建”的描述是当时的历史记录，当前不再成立。

### 已完成与实际结果

- 在单蛇虚拟环境安装 PyTorch 2.7.1+cu128，RTX 5070 Ti 的 sm_120 与 CUDA 反向更新通过验证；保留阶段一依赖锁，新增当前完整依赖锁。
- 手写 Q 网络、50000 容量环形经验池、epsilon-greedy、TD/Huber 更新、目标硬同步、梯度裁剪、独立验证、模型加载与完整恢复；无现成 RL 训练器。
- 设备独立复测：CPU 单线程约 4014 步/秒，CUDA 约 1487 步/秒，均包含 DQN 更新，本配置选择 CPU。与测试重叠的初测已单独标记。
- DQN 种子 0 完成 300000 环境步、74751 次更新、5833 个结束回合，最后 19 步未结束状态保存用于恢复。训练循环约 79.74 秒，包含验证/存档等的运行段约 104.33 秒；起点在 Trainer 构造后。
- 初始验证均值 0；最优第 260000 步为 1.30；末次 1.05。最优模型验证 12/20 碰撞、8/20 截断、0/20 满盘；弱于启发式 17.25。仅单种子验证结果。
- 轻量 initial/best/last 模型与约 83 MB 的完整 resume 存档均在本地运行目录。报告用原始数据已归档为约 2.4 MB 的可提交文件。
- 种子 1005 第 7 与第 31 步重复同一观测，此后 24 步周期持续到 2000 步，食物为 0；种子 1000 在 58 步身体碰撞，食物为 2。均有真实轨迹。
- 第二课、梯度与 TD 可运行示例、第二阶段复盘题、报告、学习曲线、更新诊断图和循环轨迹图已生成。

### 验证与修复记录

- 最终 64 项测试通过：原 50 项规则/演示检查，加 DQN 手算、停止梯度、目标冻结与同步、回放恢复、验证隔离、原回合精确恢复、截断前观测和 CLI。执行记录见 `checks_stage2.xml`。
- 本机短 CPU 测试中，在第 73 步保存后再执行 50 步，与加载完整存档后执行同样 50 步的轨迹、更新值与网络参数一致；不声称跨设备逐位一致。
- 最优模型加载后重新运行同一验证列表，食物数、步数和结束原因与训练时验证对应行一致。
- 评估元数据一开始沿用“固定策略，无训练种子”，已改为“单个冻结学习模型，训练种子见 manifest”，v2 重评结果与 v1 数值一致。历史 v1 未删除。
- 报告脚本校验训练/验证种子范围、每次 20 局、奖励分量、预算步数、源码快照哈希和循环周期；图片已目视检查。

### 下一阶段与未完成项

- 先由学生阅读第二课、运行手算示例、加载模型演示，解释 TD 目标、验证选模与绕圈问题。
- 下一阶段加入 Double DQN 的单因素对照，补齐 DQN 种子 1/2 和 Double DQN 种子 0/1/2。DQN 种子 0 既有运行保留，不能无说明重复计费或冒充新独立种子。
- 若规则或实现修复使既有运行不再可比，登记原因后重新规划受影响实验；目前基础规则未改变。
- 正式测试集仍未使用，完整单蛇模块和最终综合报告尚未完成。权重附件、commit 和 push 均等用户明确通知。

## 2026-09-10：第三阶段实验与单蛇收尾

### 已完成

- 在共享训练流程中加入 Double DQN，仅改变下一动作的选择与价值评价；其余配置、网络、奖励和预算一致。
- 复用阶段二 DQN 种子 0，新增 DQN 种子 1/2 和 Double DQN 种子 0/1/2，每次 300000 环境步，累计 1800000 步，每次 74751 次梯度更新。
- 六次均完成 31 次各 20 局验证，共 3720 条验证记录；按平均食物数最大、同分较早选模。
- `final_experiment freeze` 在最终测试前写入模型/代码/协议哈希和展示模型选择。六个 best、三个相同初始化的共用对照及两种基线共测试 220 局，之后未调参或加训。
- 单蛇 Markdown 报告包含建模、规则、算法公式、实际源码摘录、架构与流程图、训练曲线、测试图和两类真实失败案例；附逐种子配对增益解读。
- 增加第三课、带答案的复盘题和桌面演示讲解稿，更新根目录和游戏 README。个人掌握情况仍需学生亲自验收。
- 测试前按验证均值选定的 Double DQN 种子 1 best，在验证种子 1000 吃到 3 个食物，第 37 步身体碰撞；完整 GIF 和动作/观测轨迹已保存。

### 实际结果

| 方法 | 训练种子 0 / 1 / 2 的测试食物均值 | 跨种子均值 ± 样本标准差 |
| --- | --- | --- |
| 初始网络 | 0.10 / 0.10 / 0.10 | 0.100 ± 0.000 |
| DQN | 0.60 / 0.95 / 1.00 | 0.850 ± 0.218 |
| Double DQN | 0.65 / 0.80 / 1.10 | 0.850 ± 0.229 |

随机基线 20 局均值为 0.15，启发式为 19.10。两种学习算法各 60 局中均有 22 次碰撞、38 次截断、0 次满盘。三个种子均显示相对初始模型的食物数提升，但绝对觅食能力较弱；本次没有 Double DQN 的平均优势。保留该负面比较结果，没有延长预算或更换测试种子。

### 实际检查与开发取舍

- 82 项 pytest 检查通过（`snake/reports/data/checks_stage3.xml`），两种算法均验证更新、目标冻结、同步、恢复、验证隔离和演示/评估入口；新增目标数值反例、同初始化、统计单位和冻结后权重变化保护检查。
- 与阶段二归档 DQN 用相同 batch 连续五次更新，参数及指标逐位一致，Trainer 类语法树一致；因此不重复训练种子 0。每次运行仍保留各自真实源码快照。
- 报告审计已从逐局数据核对训练预算、3720 条验证记录、220 局测试、全部拆分、奖励分量、源码快照哈希和冻结早于测试的时间顺序。
- 图表经过目视检查；两种算法训练曲线统一纵轴，终局图例移到图外，防止比较误读或遮挡。报告重建只读取归档，不访问测试环境。
- 当前依赖 `pip check` 通过。权重和完整恢复文件留在本机 runs，可提交的精简日志、配置、源码快照及校验清单位于 reports/data。
- 交付审计通过：19 份 Markdown、56 个本地链接、9 个冻结评估模型；Double DQN 演示的 37 个动作逐步重放后，观测、奖励与终局信息全部一致，GIF 为 38 帧。审计入口为 `python -m snake.check_delivery`，结果见 `delivery_qa.json`。
- 正式训练与测试没有技术异常；算法碰撞和截断全部保留。一次收尾读取旧后台进程时进程句柄已失效，随后检查六次 summary 均已完成，没有重启训练或重复计入预算。

### 当前交付边界与后续

- 单蛇模块的代码、实验、报告和演示材料已交付；双蛇、马里奥及 Minecraft 未在本阶段开发，综合报告后续合并。
- 下一步建议学生按第三课手算两种目标，定位源码，加载已有模型，复盘测试表与失败轨迹，再按用户指定进入下一模块。不能把 AI 执行结果当作学生已掌握。
- 未执行 commit、push 或发布附件。公开仓库已由用户创建且本地已关联；后续操作仍等待用户明确通知。

## 2026-09-10：新增全项目体验指南约定

- 用户新增全局规则，已写入 AGENTS.md 第 9 条：每个完整游戏项目交付通俗详细的全流程使用与体验指南，覆盖奖励设计、训练、日志/可视化、模型评估、实时游玩状态与复盘，并提供实际命令、修改位置、预期输出和排错说明。
- 指南分别提供“加载已有模型体验”和“自行训练全流程”两条路径。练习不覆盖冻结结果，不把已见测试集用于调参。
- 当前单蛇模块已完成，贪吃蛇整个项目仍需双蛇同步对抗环境、自博弈、历史对手池和交叉评估。完整贪吃蛇指南将在这些部分完成后统一交付，现有单蛇讲义和演示稿继续作为阶段材料。
- 下一开发阶段为双蛇模块，先实现和验证同步动作与碰撞结算环境，再接入对手基线和自博弈训练；进入前建议先亲自体验一次已有单蛇模型并复盘核心概念。本次仅更新约定与后续安排，没有启动新训练或提交代码。

## 2026-09-10：单蛇模块源码发布

- 用户明确授权将单蛇模块提交至 GitHub，要求日志与 README 易读。本次发布范围为单蛇代码、依赖锁、检查、学习材料、报告、静态图、精简实验日志与仓库必要说明。
- 重写首页与单蛇 README，加入快速启动、真实成绩表、状态截图、阅读路线、源码地图和常见问题；区分新克隆与原开发机器、基线体验与学习模型体验。
- 新增 CHANGELOG.md 与 reports/data/README.md，说明日志文件、字段、统计单位、历史实验、模型校验与报告重建步骤。原训练和冻结数据保持不变。
- 增加 .gitattributes 以固定源码与配置的 LF 换行；暂存检查发现原始日志含 CRLF，因此原始实验数据关闭换行转换，确保冻结清单和逐局文件哈希不变。虚拟环境、缓存、权重、回放池、历史 GIF/视频和本地工具继续排除。
- 远程 main 已有用户建立的 Initial commit；本次保留其历史，追加单蛇模块提交，不强制覆盖远程分支。
- 本次不创建 GitHub Release 或版本标签，不上传模型附件；后续模块的提交与附件发布仍需用户另行通知。发布后的完整模型体验需要自行训练或后续取得权重。
- 从 Git 暂存区导出独立发布副本，未复制本机权重或虚拟环境；使用已验证的 Python 依赖运行，82 项检查通过，正式报告重建通过。157 个发布文件、78 个本地文档链接检查通过，所有原始数据字节与开发目录一致，常见凭据模式扫描无命中。
